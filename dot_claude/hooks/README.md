# Claude Hooks Setup

This directory will be populated by the official claude-hooks package.

## Installation

After chezmoi applies these files, run:

```bash
cd ~/.claude
npx claude-hooks
```

This will generate the official hooks structure with TypeScript support.

## Documentation

See <https://github.com/johnlindquist/claude-hooks> for full documentation.
