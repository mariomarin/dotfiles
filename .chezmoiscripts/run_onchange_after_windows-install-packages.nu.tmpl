#!/usr/bin/env nu
# Install packages from winget configuration DSC file (Windows)
# This script runs after files are applied, only when configuration changes
# config hash: {{ include "private_dot_config/winget/configuration.dsc.yaml" | sha256sum }}

# Check if winget is installed
if (which winget | is-empty) {
    print "❌ winget not found. Please install App Installer from Microsoft Store."
    exit 1
}

# Use just target for actual configuration
cd {{ .chezmoi.sourceDir }}
just windows-configure
