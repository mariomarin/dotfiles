#compdef cl

# Completion for cl (claude with MCP container-use enforcement)
# Inherits from _claude but with MCP-specific defaults

_arguments -s -S -C \
    {-h,--help}'[Show help information]' \
    {-V,--version}'[Show version information]' \
    '--model[Specify model to use]:model:(opus sonnet haiku)' \
    '--allowed-tools[Override default container-use tools]:tools' \
    '--append-system-prompt[Append to system prompt]:prompt' \
    '--output-format[Output format]:format:(text json stream-json)' \
    '--print[Print output]' \
    '--verbose[Verbose output]' \
    '--temperature[Set model temperature (0-1)]:temperature' \
    '--max-tokens[Maximum tokens in response]:tokens' \
    '*::prompt:_default'

return 0