# -------
# Modules
# -------

# Sets sane Zsh built-in environment options.
zmodule environment
# Provides handy git aliases and functions.
zmodule git
# Applies correct bindkeys for input events.
zmodule input
# Sets a custom terminal title.
zmodule termtitle
# Utility aliases and functions. Adds colour to ls, grep and less.
zmodule utility
zmodule exa
zmodule fzf
# wakatime
zmodule sobolevn/wakatime-zsh-plugin

# Prompt theme
zmodule prompt-pwd
zmodule git-info
zmodule gitster

# Additional completion definitions for Zsh.
zmodule zsh-users/zsh-completions --fpath src

# Custom modules
# ssh-agent - SSH agent socket detection (forwarded → GNOME → systemd)
zmodule ${ZIM_CONFIG_FILE:h}/modules/ssh-agent
# sesh - Smart session manager with tmux integration
zmodule ${ZIM_CONFIG_FILE:h}/modules/sesh
# claude-helpers - Claude Code helper functions
zmodule ${ZIM_CONFIG_FILE:h}/modules/claude-helpers

# Enables and configures smart and extensive tab completion.
# Must load before carapace
zmodule completion

# Carapace - Universal completion framework
# Provides completions for container-use, aws-sso-cli, git-branchless, and many more
# Must be initialized after completion module (needs compdef)
zmodule carapace-sh/carapace-bin --on-pull 'carapace _carapace > init.zsh'
# atuin
zmodule atuinsh/atuin

#
# Modules that must be initialized last
#
# Fish-like syntax highlighting for Zsh.
# zsh-users/zsh-syntax-highlighting must be sourced after completion
zmodule zsh-users/zsh-syntax-highlighting
# A better and friendly vi(vim) mode.
# NOTE: must be sourced after highlighting
zmodule jeffreytse/zsh-vi-mode
# Fish-like history search (up arrow) for Zsh.
# zsh-users/zsh-history-substring-search must be sourced after zsh-users/zsh-syntax-highlighting
zmodule zsh-users/zsh-history-substring-search
# Fish-like autosuggestions for Zsh.
zmodule zsh-users/zsh-autosuggestions

