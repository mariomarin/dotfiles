{{- if eq (index .machineConfig "platform" | default "") "darwin-brew" -}}
# Generated by chezmoi - do not edit
# Install with: brew bundle --file=~/.config/brew/Brewfile

# Taps
tap "homebrew/bundle"
tap "homebrew/services"
tap "nikitabobko/tap" # aerospace

# Formulas
{{ range .brew -}}
brew "{{ . }}"
{{ end }}
# Casks
{{ range .brew_casks -}}
cask "{{ . }}"
{{ end -}}
{{- end -}}
