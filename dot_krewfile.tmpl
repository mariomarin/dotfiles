# Krewfile - Declarative kubectl plugin management
# Topgrade automatically updates krew plugins when you run: topgrade or just update
# To install plugins from this file: just krew-sync

{{- if ne .chezmoi.os "windows" }}
# Context and namespace management (not available on Windows)
# Use kubectx/kubens from packages.yaml instead
ctx
ns

# Resource exploration and debugging (Unix only)
neat
node-shell

# Security and policy (Unix only)
rbac-lookup

{{- end }}
# Resource exploration and debugging (cross-platform)
stern
tree

# Security and policy (cross-platform)
access-matrix

# Resource management (cross-platform)
resource-capacity
